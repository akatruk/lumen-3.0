# Render summary
Approved in conversation: show the actual saved render plan before Create video. Include source/output duration, removed ranges, ordered shots and executable effects, audio and captions, unapplied AI proposals, and a conservative near-original warning. Keep existing cost disclosure and explicit render confirmation. No AI call is required to summarize. Fetch from an authenticated endpoint on each dialog opening; fail closed on loading/error/revision mismatch. Rendering still checks revision atomically. Source audio is used; separate dubbed outputs are not silently applied.

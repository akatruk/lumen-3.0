# Task-focused right inspector

User requested a high-quality redesign of the right Tools panel, continuing the authorized shared UX work. Implement directly within existing editing behavior.

Use a flat inspector rather than nested card borders. Scene navigation and approval precede task controls; effects presets are prominent visual choices. Precision framing, motion, transitions, text and inserts are clearly separated. Reordering/deletion and help are secondary. Existing save/render confirmation, locks, translations and draft persistence remain authoritative. Preview should use available desktop height instead of shrinking to a 240px postage stamp. Keep mobile controls at least 40px high, no horizontal overflow, and preserve keyboard labels/focus.

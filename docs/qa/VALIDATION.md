# Validation — 2026-09-14

## Automated

- TypeScript check + Vite production build: passed.
- Dependency audit from npm install: 0 reported vulnerabilities.
- API/media unit suite: account isolation, authentication, Origin checks, invalid media, pending job limit, atomic budget reservations, deletion without budget reset, duplicate retry exclusion.
- Provider contract suite: insufficient credits produce an explicit error with zero settled cost; absent keys reserve nothing; malformed output fails closed; quality review receives original and final videos.
- Real FFmpeg regression on Ubuntu 24.04: cuts, source reordering verified by decoded pixels, Chinese ASS subtitles, audio preservation, duration and decoding checks. Passed. Chinese characters inspected in a rendered frame.
- Local Homebrew FFmpeg lacks libass. Do not interpret the local renderer limitation as a production renderer pass; the complete render test ran on the target Ubuntu VM.

## Browser

- Local built app: login with isolated QA account, English and Simplified Chinese navigation.
- Real upload of an 8-second synthesized video through the file chooser and multipart upload.
- Durable project appears in the project library after reload.
- Actual provider request returns 402 and UI displays the insufficient-credit explanation; no fabricated recommendations or completed output.
- Explicit local fixture, labeled "not AI analysis", used for recommendation expansion, checkbox selection and source timestamp seeking. It is not production content.

## Deployment

- New host: 142.93.248.163. Old VM was not accessed successfully and was not changed.
- Public HTTPS and HTTP→HTTPS redirect verified.
- /api/health returns status ok, version 0.1.0.
- lumen-web, lumen-worker and nginx active.
- Secrets file root:lumen 0640; media directory lumen:lumen 0700.
- Certificate issued for lumen-fix.universalgravity.org, expiry 2026-12-13; renewal managed by certbot.

## Live synthetic speech test

- A 28-second, computer-narrated test video completed real multimodal analysis, speech transcription, three automatic edits, real MP4 export and independent original/output review.
- Model identified initial/trailing dead time and captions; automatic render duration 16.341 seconds, source 28 seconds.
- First analysis + quality review cost: $0.03497325.
- Quality review flagged an orphaned one-word subtitle card and a source color/narration mismatch. The result was correctly marked needs_review. Subtitle card balancing was fixed and regression-tested.
- Complete regression suite before final line-balancing correction: 18 passed on Ubuntu, run as the service user lumen.

## Not yet validated

Semantic accuracy and creative quality on representative customer videos still require real evaluation material and human review. API contract mocks and a synthetic narrated clip do not establish those properties.

## Live generated footage

- One four-second first-frame-guided clip generated by the configured provider and composited for the recommended three-second interval while retaining source audio.
- Total project spend across initial analysis, both renders/reviews and generated clip: $0.3693005.
- AI review confirmed readable, synchronized captions after the fix; it still flagged the generated background continuity and source color/narration mismatch. The application correctly retained needs_review.
- This establishes functional integration, not creative acceptance.

## Final verification

19 tests passed on the target Ubuntu VM as service user lumen after the final caption-line balancing fix. Public frontend console reported no JavaScript errors. With explicit user approval, the remote temporary QA account, sessions, synthetic project and media were deleted. Spend history and local regression artifacts were retained. The public health endpoint returned OK after cleanup.
